"""Built-in Agent Skills shipped with the app.

Each subdirectory is one skill bundle discovered via its ``SKILL.md``
(frontmatter name + description) and optional ``manifest.yaml``. A bundle
may additionally be an importable Python package (``skills.citation``) when
the skill owns runtime code; plain instruction-only bundles need no Python
files at all.
"""
